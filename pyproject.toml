[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "guardian-ai"
version = "0.1.0"
description = "Guardian-AI ransomware-response MVP prototype"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
